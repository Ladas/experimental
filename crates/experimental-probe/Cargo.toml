[package]
name = "experimental-probe"
description = "Probe crate that verifies the experimental workspace toolchain, lint, and security configuration"
repository = "https://github.com/praxis-proxy/experimental"
homepage = "https://github.com/praxis-proxy/experimental"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
